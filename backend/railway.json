{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --no-input && python manage.py collectstatic --no-input --clear && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --worker-class gthread --workers 4 --threads 4"
  }
}